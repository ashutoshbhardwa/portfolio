// Stub — real data loaded at runtime from /public/branch-lines.json
// These zero-length arrays satisfy TS imports; JSON fetch populates actual geometry.
export const BRANCH_SEG_COUNT = 0;
export const BRANCH_POSITIONS = new Float32Array(0);
export const BRANCH_DELAYS    = new Float32Array(0);
export const BRANCH_ALPHAS    = new Float32Array(0);
export const BRANCH_PHASES    = new Float32Array(0);
